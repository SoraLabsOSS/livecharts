# @livecharts/react

React bindings for LiveCharts (`LiveChart`, `LiveChartTransition`).

Published via the public package subpath:

```ts
import { LiveChart } from "livecharts/react";
```

This workspace package is **private** — consumers install `livecharts`.
